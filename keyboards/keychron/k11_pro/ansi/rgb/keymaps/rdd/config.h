#pragma once

// Set the global default tapping term to 200ms
#define TAPPING_TERM 200

// Enable per-key tapping terms to allow 100ms on your Esc key
#define TAPPING_TERM_PER_KEY

// Makes Mod-Taps snappier for fast typists
#define PERMISSIVE_HOLD

// Replaces the deprecated TAPPING_FORCE_HOLD. 
// Set to 0 to activate the hold function when quickly tapping and holding.
#define QUICK_TAP_TERM 0